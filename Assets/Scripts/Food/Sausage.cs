using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sausages : FoodItem
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("hello, I am sausage");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
